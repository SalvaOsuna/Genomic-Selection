setwd("~/GitHub/Genomic-Selection/Genomic Selection_SALVA/data")
DarT <- read.table("Markers.txt",sep= "\t",head=FALSE)
DarT <- read.table("Markers.txt",sep= "\t",head=T)
DarT <- read.table("Markers.txt",sep= "\t",head=T)
DarT <- read.table("Markers.txt",sep= "\t",head=F)
DarT <- read.table("Markers.txt")
DarT <- read.table("Markers.txt",sep= "\t",head=FALSE)
View(DarT)
DarT <- read.table("Markers.txt",sep= "\t",head=T)
View(DarT)
DarT <- data.frame(DarT)
DarT[DarT == 0] <- -1
DarT[DarT == NA] <- 0
DarT[DarT == is.na()] <- 0
is.na(DarT)
DarT[DarT == is.na(DarT)] <- 0
DarT[is.na(DarT)] <- 0
library(tidyverse)
DarT %>%
count()
DarT %>%
count(X.Marker.)
#MAF (5%)
t(DarT)
#MAF (5%)
DarT <- t(DarT)
#MAF (5%)
DarT <- t(DarT, head = T)
#MAF (5%)
names(DarT) <- dat[1,]
#MAF (5%)
names(DarT) <- DarT[1,]
DarT <- DarT[-1,]
DarT <- read.table("Markers.txt",sep= "\t",head=T)
DarT <- data.frame(DarT)
#Substitute 0 to -1 (homozygous minor aa) and  NA to 0 (heterozigosis aA or Aa)
DarT[DarT == 0] <- -1
DarT[is.na(DarT)] <- 0
names(DarT) <- as.matrix(DarT[1, ])
DarT <- DarT[-1, ]
DarT[] <- lapply(DarT, function(x) type.convert(as.character(x)))
DarT <- read.table("Markers.txt",sep= "\t",head=T)
DarT <- data.frame(DarT)
#Substitute 0 to -1 (homozygous minor aa) and  NA to 0 (heterozigosis aA or Aa)
DarT[DarT == 0] <- -1
DarT[is.na(DarT)] <- 0
#MAF (5%)
t(DarT)
names(DarT) <- as.matrix(DarT[1, ])
DarT <- DarT[-1, ]
DarT[] <- lapply(DarT, function(x) type.convert(as.character(x)))
DarT <- read.table("Markers.txt",sep= "\t",head=T)
DarT <- data.frame(DarT)
#Substitute 0 to -1 (homozygous minor aa) and  NA to 0 (heterozigosis aA or Aa)
DarT[DarT == 0] <- -1
DarT[is.na(DarT)] <- 0
# 4) Filter data:
#Missing per marker and missing per sample
# It seems that there is not missing values in the marker/sample
#MAF (5%)
DarT <-t(DarT)
names(DarT) <- as.matrix(DarT[1, ])
DarT <- DarT[-1, ]
DarT[] <- lapply(DarT, function(x) type.convert(as.character(x)))
DarT <- read.table("Markers.txt",sep= "\t",head=T)
DarT <- data.frame(DarT)
#Substitute 0 to -1 (homozygous minor aa) and  NA to 0 (heterozigosis aA or Aa)
DarT[DarT == 0] <- -1
DarT[is.na(DarT)] <- 0
# 4) Filter data:
#Missing per marker and missing per sample
# It seems that there is not missing values in the marker/sample
#MAF (5%)
DarT <-t(DarT)
DarT <- read.table("Markers.txt",sep= "\t",head=F)
DarT <- data.frame(DarT)
#Substitute 0 to -1 (homozygous minor aa) and  NA to 0 (heterozigosis aA or Aa)
DarT[DarT == 0] <- -1
DarT[is.na(DarT)] <- 0
# 4) Filter data:
#Missing per marker and missing per sample
# It seems that there is not missing values in the marker/sample
#MAF (5%)
DarT <-t(DarT)
head(DarT)
names(DarT) <- as.matrix(DarT[1, ])
names(DarT)
DarT <- DarT[-1, ]
DarT[] <- lapply(DarT, function(x) type.convert(as.character(x)))
DarT <- read.table("Markers.txt",sep= "\t",head=F)
DarT <- data.frame(DarT)
#Substitute 0 to -1 (homozygous minor aa) and  NA to 0 (heterozigosis aA or Aa)
DarT[DarT == 0] <- -1
DarT[is.na(DarT)] <- 0
# 4) Filter data:
#Missing per marker and missing per sample
# It seems that there is not missing values in the marker/sample
#MAF (5%)
DarT <-t(DarT)
#set first row as header:
install.packages("janitor")
library(janitor)
row_to_names(DarT)
row_to_names(DarT)
row_to_names(dat = DarT, row_number = 1)
DarT <- row_to_names(dat = DarT, row_number = 1)
DarT <- data.frame(row_to_names(dat = DarT, row_number = 1))
DarT <- read.table("Markers.txt",sep= "\t",head=F)
DarT <- data.frame(DarT)
#Substitute 0 to -1 (homozygous minor aa) and  NA to 0 (heterozigosis aA or Aa)
DarT[DarT == 0] <- -1
DarT[is.na(DarT)] <- 0
DarT <-t(DarT)
DarT <- data.frame(DarT, row.names = [,1])
DarT <- data.frame(DarT, row.names = c(DarT[,1]))
DarT <- data.frame(DarT, row.names = c(DarT[1,]))
DarT <- data.frame(DarT, row.names = 1)
DarT <- DarT %>%
arrange(V1)
DarT <- data.frame(DarT)
DarT <- DarT %>%
arrange(X1)
DarT <- read.table("Markers.txt",sep= "\t",head=F)
DarT <- data.frame(DarT)
#Substitute 0 to -1 (homozygous minor aa) and  NA to 0 (heterozigosis aA or Aa)
DarT[DarT == 0] <- -1
DarT[is.na(DarT)] <- 0
DarT <-t(DarT)
write.table("Dart.txt")
write.table(DarT, file = "Dart.txt", ,sep= "\t")
library(openxlsx)
DarT1 <- read.xlsx("DarT.xlsx", sep= "\t",head=T)
DarT1 <- read.xlsx("DarT.xlsx", sep= "\t" ,colNames = T)
View(DarT1)
DarT1 %>%
count(1)
DarT1 %>%
count(2)
DarT1 %>%
count()
DarT1 %>%
count("1")
DarT1 %>%
count("2")
DarT1 %>%
count("3534449")
